<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Sistema de Siniestros Viales{% endblock %}</title>

    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa;
        }
        .navbar {
            background-color: #0d3b66 !important; /* Azul policía oscuro */
        }
        .navbar .navbar-brand, .navbar .nav-link, .navbar-text {
            color: white !important;
        }
        footer {
            background-color: #0d3b66;
            color: white;
            padding: 10px 0;
            text-align: center;
            margin-top: 40px;
        }
        .content-wrapper {
            padding-top: 20px;
        }
        .btn-primary {
            background-color: #145ea8;
            border-color: #145ea8;
        }
        .btn-primary:hover {
            background-color: #0d3b66;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>

    {% if app.user %}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="{{ path('siniestro_list') }}">🚓 Siniestros Viales</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">

                <!-- Siniestros -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="siniestrosDropdown" role="button" data-bs-toggle="dropdown">
                        Siniestros
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('siniestro_list') }}">📋 Listado</a></li>
                        <li><a class="dropdown-item" href="{{ path('siniestro_new') }}">➕ Nuevo</a></li>
                    </ul>
                </li>

                <!-- Detalles -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="detallesDropdown" role="button" data-bs-toggle="dropdown">
                        Detalles
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('detalle_list') }}">📋 Listado</a></li>
                        <li><a class="dropdown-item" href="{{ path('detalle_new') }}">➕ Nuevo</a></li>
                    </ul>
                </li>

                <!-- Personas -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="personasDropdown" role="button" data-bs-toggle="dropdown">
                        Personas
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('persona_list') }}">📋 Listado</a></li>
                        <li><a class="dropdown-item" href="{{ path('persona_new') }}">➕ Nueva</a></li>
                    </ul>
                </li>

                <!-- Reportes -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="reportesDropdown" role="button" data-bs-toggle="dropdown">
                        Reportes
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('reporte_siniestros_mes') }}">📅 Siniestros por Mes</a></li>
                        <li><a class="dropdown-item" href="{{ path('reporte_roles_siniestros') }}">👤 Roles en Siniestros</a></li>
                        <li><a class="dropdown-item" href="{{ path('reporte_por_localidad') }}">📍 Por Localidad</a></li>
                        <li><a class="dropdown-item" href="{{ path('reporte_por_sexo') }}">🚻 Por Sexo</a></li>
                        <li><a class="dropdown-item" href="{{ path('reporte_tipo_vehiculo') }}">🚗 Por Tipo de Vehículo</a></li>
                        <li><a class="dropdown-item" href="{{ path('reporte_estado_alcoholico') }}">🍷 Estado Alcohólico</a></li>
                    </ul>
                </li>
            </ul>

            <ul class="navbar-nav">
                <li class="nav-item">
                    <span class="navbar-text text-light me-3">
                        👋 Hola <strong>{{ app.user.email }}</strong>
                        <small class="text-secondary">(
                            {{ app.user.roles|first|replace({'ROLE_':''})|capitalize }}
                        )</small>
                    </span>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-light btn-sm">Salir</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
{% endif %}


    <div class="container content-wrapper">
        {% block body %}{% endblock %}
    </div>

    <footer>
        <div>© {{ "now"|date("Y") }} - Sistema de Siniestros Viales</div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    {% block javascripts %}{% endblock %}
</body>
</html>


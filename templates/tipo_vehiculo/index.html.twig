{% extends 'base.html.twig' %}

{% block title %}Listado de Tipos de Vehículo{% endblock %}

{% block body %}
<div class="card shadow-sm p-4">
    <h2 class="text-primary mb-3">Listado de Tipos de Vehículo</h2>

    <table class="table table-striped table-hover">
        <thead class="table-primary">
            <tr>
                <th>ID</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            {% for tipo_vehiculo in tipo_vehiculos %}
                <tr>
                    <td>{{ tipo_vehiculo.id }}</td>
                    <td>{{ tipo_vehiculo.descripcion }}</td>
                    <td>
                        <a href="{{ path('app_tipo_vehiculo_show', {'id': tipo_vehiculo.id}) }}" class="btn btn-sm btn-info text-white">Ver</a>
                        <a href="{{ path('app_tipo_vehiculo_edit', {'id': tipo_vehiculo.id}) }}" class="btn btn-sm btn-warning text-white">Editar</a>
                        <form method="post" action="{{ path('app_tipo_vehiculo_delete', {'id': tipo_vehiculo.id}) }}" style="display:inline;" onsubmit="return confirm('¿Seguro que quieres eliminar este tipo de vehículo?');">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_vehiculo.id) }}">
                            <button class="btn btn-sm btn-danger">🗑️ Eliminar</button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3" class="text-center text-muted">No hay tipos de vehículo registrados.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tipo_vehiculo_new') }}" class="btn btn-success">➕ Nuevo Tipo de Vehículo</a>
</div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Inicio - Siniestros Viales{% endblock %}

{% block body %}
<div class="d-flex align-items-center justify-content-center vh-100 bg-dark text-light">
    <div class="text-center">
        <h1 class="fw-bold mb-3">🚓 Sistema de Gestión de Siniestros Viales</h1>
        <p class="lead mb-4">Registrá, gestioná y analizá siniestros de forma simple y segura.</p>

        <div class="d-flex justify-content-center gap-3">
            <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg px-4">
                🔑 Iniciar Sesión
            </a>
            <a href="{{ path('app_register') }}" class="btn btn-outline-light btn-lg px-4">
                📝 Registrarse
            </a>
        </div>
    </div>
</div>
{% endblock %}

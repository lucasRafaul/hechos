{% extends 'base.html.twig' %}

{% block title %}Detalle del Siniestro{% endblock %}

{% block body %}
<div class="card shadow-sm p-4">
    <h2 class="text-primary mb-4">📋 Detalle del Siniestro</h2>

    <div class="row mb-3">
        <div class="col-md-6">
            <p><strong>ID:</strong> {{ siniestro.id }}</p>
            <p><strong>Fecha:</strong> {{ siniestro.fecha|date('Y-m-d') }}</p>
            <p><strong>Ubicación:</strong> {{ siniestro.ubicacion }}</p>
        </div>
        <div class="col-md-6">
            <p><strong>Localidad:</strong> {{ siniestro.localidad ? siniestro.localidad.nombre : 'Sin localidad' }}</p>
            <p><strong>Clima:</strong> {{ siniestro.clima ? siniestro.clima.descripcion : 'Sin clima' }}</p>
            <p><strong>Descripción:</strong> {{ siniestro.descripcion }}</p>
        </div>
    </div>

    {% if siniestro.detalleSiniestros is defined and siniestro.detalleSiniestros|length > 0 %}
        <h4 class="mt-4">🚗 Detalles asociados</h4>
        <table class="table table-striped table-hover mt-3">
            <thead class="table-primary">
                <tr>
                    <th>Vehículo</th>
                    <th>Rol</th>
                </tr>
            </thead>
            <tbody>
                {% for detalle in siniestro.detalleSiniestros %}
                    <tr>
                        <td>{{ detalle.tipo_vehiculo }}</td>
                        <td>{{ detalle.rol }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p class="text-muted mt-3">No hay detalles asociados.</p>
    {% endif %}

    <div class="mt-4 d-flex gap-2">
        <a href="{{ path('siniestro_edit', {'id': siniestro.id}) }}" class="btn btn-warning">✏️ Editar</a>
        <a href="{{ path('siniestro_index') }}" class="btn btn-secondary">🔙 Volver al listado</a>
    </div>
</div>
{% endblock %}
